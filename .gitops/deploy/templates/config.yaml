apiVersion: pkg.crossplane.io/v1
kind: Configuration
metadata:
  name: helm-knative-operator
spec:
  package: ghcr.io/hops-ops/helm-knative-operator:{{ .Values.version }}
  packagePullSecrets:
    - name: ghcr
  skipDependencyResolution: {{ .Values.skipDependencyResolution }}

{{- if .Values.skipDependencyResolution }}
---
# Only rendered when skipDependencyResolution=true (platform/advanced use case)
# When false, Crossplane automatically resolves and installs embedded functions
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: hops-ops-helm-knative-operatorrender
spec:
  package: ghcr.io/hops-ops/helm-knative-operator_render:{{ .Values.version }}
  packagePullSecrets:
    - name: ghcr
  skipDependencyResolution: {{ .Values.skipDependencyResolution }}
{{- end }}
